{
  "name": "popclip-flomo-extension",
  "version": "1.0.0",
  "description": "PopClip extension for saving selected text to flomo",
  "main": "Config.ts",
  "scripts": {
    "build": "tsc Config.ts --outDir dist"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}